<% unless room.id.nil? %>
  <li class="list-group-item" id="<%= dom_id room %>" data-controller="rooms">
    <%= link_to room_set_room_path(room), method: :post, data: { action: 'click->rooms#loadRoom' } do %>
      <%= room.name %>
    <% end %>
  </li>
<% end %>